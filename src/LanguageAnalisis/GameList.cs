public struct GameList    //Contiene temporalmente a las cartas ya creadas a partir de un código específico para su posterior análisis
{
    public static Card[] AllCards = new Card[]{};
    public static Action[] AllActions = new Action[]{};
    public static Effect[] AllEffects = new Effect[]{};
    public static Condition[] AllConditions = new Condition[]{};
}
